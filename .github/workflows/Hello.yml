name: CI Smoke Test

on:
  push:
  pull_request:

jobs:
  smoke:
    name: Smoke Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Say hello
        run: echo "GitHub Actions está funcionando 🚀✴️"